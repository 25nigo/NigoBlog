---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { getCollection } from 'astro:content';
// consts.ts からタイトルと説明を読み込み
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';

const posts = await getCollection('posts');

// シリーズごとに記事をフィルタリング
const mobaPosts = posts.filter(post => post.data.series === 'moba');
const diyPosts = posts.filter(post => post.data.series === 'diy');
---

<!doctype html>
<html lang="ja">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <body>
    <Header />
    <main>
      <section>
        <h1>{SITE_TITLE}</h1>
        <p>{SITE_DESCRIPTION}</p>
      </section>

      <section>
        <h2>🎮 MOBAの持論</h2>
        <ul>
          {mobaPosts.map((post) => (
            <li>
             <a href={`/posts/${post.slug}/`}>{post.data.title}</a>
            </li>
          ))}
        </ul>
      </section>

      <section>
        <h2>🛠️ DIYの記録</h2>
        <ul>
          {diyPosts.map((post) => (
            <li>
              <a href={`/posts/${post.slug}/`}>{post.data.title}</a>
            </li>
          ))}
        </ul>
      </section>
    </main>
    <Footer />
    
    <style>
      /* モバイルで見やすくするための簡単な調整 */
      main {
        width: 100%;
        max-width: 720px;
        margin: auto;
        padding: 1em;
      }
      ul {
        list-style-type: none;
        padding: 0;
      }
      li {
        margin-bottom: 1rem;
        border-bottom: 1px solid #eee;
        padding-bottom: 0.5rem;
      }
      a {
        text-decoration: none;
        color: #0076d1;
        font-weight: bold;
      }
    </style>
  </body>
</html>